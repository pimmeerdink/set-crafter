# set-crafter API runtime env (loaded by systemd via EnvironmentFile).
PYTHONUNBUFFERED=1

# python_app's systemd hardening sets ProtectHome=read-only, so $HOME isn't
# writable. Point uv's cache at the writable repo dir.
UV_CACHE_DIR=/opt/set-crafter-api/repo/.uv-cache
