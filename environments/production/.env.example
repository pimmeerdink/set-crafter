# Copy to environments/production/.env and fill in real values.
# This file is committed; .env is not.

# Cloudflare — DNS for socialtechnologylab.nl (Zone:Read + DNS:Edit)
CLOUDFLARE_API_TOKEN=

# GitHub repo to deploy from (set-crafter)
GITHUB_REPO=git@github.com:pimmeerdink/set-crafter.git
